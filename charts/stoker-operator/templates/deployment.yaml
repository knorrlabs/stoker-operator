apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stoker-operator.fullname" . }}-controller-manager
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stoker-operator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "stoker-operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "stoker-operator.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: {{ include "stoker-operator.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /manager
          args:
            {{- if .Values.leaderElection.enabled }}
            - --leader-elect
            {{- end }}
            - --health-probe-bind-address=:8081
          {{- if .Values.metrics.enabled }}
            - --metrics-bind-address=:{{ .Values.metrics.service.port }}
          {{- end }}
          {{- if and .Values.webhook.enabled .Values.certManager.enabled }}
            - --webhook-cert-path=/etc/webhook-certs
          {{- end }}
            - --webhook-receiver-port={{ if .Values.webhookReceiver.enabled }}{{ .Values.webhookReceiver.port | default 9444 }}{{ else }}0{{ end }}
          env:
            - name: DEFAULT_AGENT_IMAGE
              value: "{{ .Values.agentImage.repository }}:{{ .Values.agentImage.tag | default .Chart.AppVersion }}"
            - name: AUTO_BIND_AGENT_RBAC
              value: {{ .Values.rbac.autoBindAgent.enabled | quote }}
            - name: LOG_DEV_MODE
              value: {{ .Values.controller.logDevMode | quote }}
            {{- if and .Values.webhookReceiver.enabled .Values.webhookReceiver.hmac.secretRef.name }}
            - name: WEBHOOK_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.webhookReceiver.hmac.secretRef.name }}
                  key: {{ .Values.webhookReceiver.hmac.secretRef.key }}
            {{- else if and .Values.webhookReceiver.enabled .Values.webhookReceiver.hmac.secret }}
            - name: WEBHOOK_HMAC_SECRET
              value: {{ .Values.webhookReceiver.hmac.secret | quote }}
            {{- end }}
            {{- if and .Values.webhookReceiver.enabled .Values.webhookReceiver.token.secretRef.name }}
            - name: WEBHOOK_BEARER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.webhookReceiver.token.secretRef.name }}
                  key: {{ .Values.webhookReceiver.token.secretRef.key }}
            {{- else if and .Values.webhookReceiver.enabled .Values.webhookReceiver.token.secret }}
            - name: WEBHOOK_BEARER_TOKEN
              value: {{ .Values.webhookReceiver.token.secret | quote }}
            {{- end }}
          ports:
          {{- if .Values.metrics.enabled }}
            - containerPort: {{ .Values.metrics.service.port }}
              name: metrics
              protocol: TCP
          {{- end }}
          {{- if .Values.webhook.enabled }}
            - containerPort: {{ .Values.webhook.port | default 9443 }}
              name: webhook-server
              protocol: TCP
          {{- end }}
          {{- if .Values.webhookReceiver.enabled }}
            - containerPort: {{ .Values.webhookReceiver.port | default 9444 }}
              name: webhook-rx
              protocol: TCP
          {{- end }}
          {{- if not (or .Values.webhook.enabled .Values.webhookReceiver.enabled) }}
            []
          {{- end }}
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - "ALL"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            {{- if and .Values.webhook.enabled .Values.certManager.enabled }}
            - mountPath: /etc/webhook-certs
              name: cert
              readOnly: true
            {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if and .Values.webhook.enabled .Values.certManager.enabled }}
        - name: cert
          secret:
            defaultMode: 420
            secretName: {{ include "stoker-operator.fullname" . }}-webhook-cert
        {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "stoker-operator.serviceAccountName" . }}
      terminationGracePeriodSeconds: 10
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
